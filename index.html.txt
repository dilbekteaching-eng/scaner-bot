<!DOCTYPE html>
<html lang="uz">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Skaner</title>
    <script src="https://telegram.org/js/telegram-web-app.js"></script>
    <script src="https://unpkg.com/@zxing/library@latest"></script>
    <style>
        body { font-family: sans-serif; text-align: center; margin: 0; padding: 20px; background-color: #f4f4f9; }
        #video { width: 100%; max-width: 500px; border: 5px solid #0088cc; border-radius: 15px; }
        .status { margin-top: 15px; font-weight: bold; color: #555; }
    </style>
</head>
<body>
    <h2>Mahsulotni skanerlang</h2>
    <video id="video"></video>
    <div class="status" id="status">Kamerani shtrix-kodga qarating...</div>

    <script>
        const codeReader = new ZXing.BrowserMultiFormatReader();
        const statusEl = document.getElementById('status');
        const tg = window.Telegram.WebApp;

        // Telegram Web App-ni kengaytirish
        tg.expand();

        codeReader.decodeFromVideoDevice(null, 'video', (result, err) => {
            if (result) {
                // Kod topilganda botga yuborish
                statusEl.innerText = "Kod aniqlandi: " + result.text;
                tg.sendData(result.text); // Ma'lumotni botga yuboradi
                tg.close(); // WebApp-ni yopadi
            }
        });
    </script>
</body>
</html>